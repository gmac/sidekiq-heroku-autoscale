api_token: n0th1ngt0s33h3r3
app_name: test-app
sidekiq_autostart: true
processes:
  first:
    system:
      watch_queues:
        - default
        - low
      include_retrying: false
      include_scheduled: false
    scale:
      mode: binary
      max_workers: 2
    throttle: 15
    quiet_buffer: 15
    minimum_uptime: 15

  second:
    system:
      watch_queues:
        - high
      include_retrying: false
      include_scheduled: false
    scale:
      mode: linear
      max_workers: 5
      worker_capacity: 50
      min_factor: 1
    throttle: 20
    quiet_buffer: 20
    minimum_uptime: 20